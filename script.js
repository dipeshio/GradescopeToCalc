
// create a function that takes the current web page and extracts the name of assignments, and dates due,
// and returns an array of objects with the name and date of each assignment.
// Title of assignments is data-assignment-title in the html, class js-submitAssignment
// ,due date is class submissionTimeChart--dueDate


function printWebName() {
    // popup with current URL
    alert(window.location.href);
}

function testFunction() {
    // create a simple popup with the text "Hello World!"
    alert("Hello World!");
}